# AGENTSガイドライン

- 変更には自動テストを必ず追加・更新し、`npm test` などで実行して検証すること。
- ソースファイルは単一責任の原則を守り、肥大化させない。特に500行を超えた場合は分割を優先して検討すること。
- コミット前に `npm run lint` と `npm run format` を実行し、必要に応じて `npm test` や `npm run build` で主要な検証を完了させること。
